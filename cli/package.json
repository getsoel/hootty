{
  "name": "hootty-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --target=bun-darwin-arm64 src/index.ts --outfile ../Sources/Hootty/Resources/bin/hootty",
    "dev": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7"
  }
}
